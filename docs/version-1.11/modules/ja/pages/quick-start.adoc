include::partial$variables.adoc[]

= クイックスタート
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2026-09-04
:page-revdate: {revdate}
:description: {project-name}を使用して始めるには、{project-name}スタックをインストールし、前提条件と認証を行います。

このクイックスタートでは、Ollamaを使用して{short-project-name}にLLM機能を提供する方法を説明します。初回インストール後は、xref:/how-tos/how-to-admin.adoc[管理者向けハウツー]を参照することでプロバイダーを切り替えることができます。

== 前提条件

現在のAIアシスタントのバージョンには、以下のコンポーネントが必要です：

* https://helm.sh/docs/[Helmがインストールされていること]
* Rancher Prime/Community Manager 2.13以上

ホストクラスターでCRD（cluster_admin）をデプロイする権限があることを確認してください。

== 技術要件

サポートされているAIコンポーネントとその要件の表を以下に示します。

[NOTE]
====

実行する特定のコンポーネントの要件を満たす必要があります。要件は、デプロイする特定の大規模言語モデル（LLM）によって異なります。

====

[cols="1,1,1", options="header"]
|===
| LLMモデル | 要件 | GPU要件

| https://ollama.com/library/gpt-oss[gpt-oss:20b]
| Ollamaがインストールされていること
| NVIDIA RTX A5000、NVIDIA RTX 4090または同等（最小24Gb VRAM）


| https://ollama.com/library/gpt-oss[gpt-oss:120b]
| Ollamaがインストールされていること
| NVIDIA A100または同等（最小80Gb VRAM）

| Gemini
| Google Workspaceアカウント
| N/A

| ChatGPT
| OpenAIアカウント
| N/A

| AWS Bedrock
| AWSアカウント
| N/A

| OpenAIのようなプロバイダー
| Azureアカウント、Evroc Think、...
| N/A
|===

[IMPORTANT]
====

テスト済みモデルのリストは、xref:/explanations/models.adoc[モデル]のドキュメントで入手できます。

====

[IMPORTANT]
====

自分のOllamaを実行する場合は、ローカルモデルのために少なくとも`gpt-oss:20b`を用意してください。モデルを取得する方法については、 https://ollama.com/library[Ollamaのドキュメント]を参照してください。

====

== {project-name}をインストールします。

{short-project-name}のインストールは二段階のプロセスです：

* 提供されたHelmチャートを使用してエージェントとMCPをデプロイします。
* Rancher UI拡張をデプロイします。

=== ローカルクラスターにエージェントとMCPをインストールします。

. `values.yaml`ファイル設定を作成します（Ollama用）：
+
[source,yaml]
----
ollamaLlmModel: "gpt-oss:20b"
ollamaUrl: "http://ollama:11434"
activeLlm: "ollama"
----
+
[NOTE]
====

後でRancherの「グローバル設定-> AIアシスタントタブ」でこれらの設定とプロバイダーを変更できます。

====
. rancher-ai-agentチャートをインストールします。
+
[source,bash,subs="attributes+"]
----
helm install rancher-ai-agent \
  --namespace cattle-ai-agent-system \
  --create-namespace \
  -f values.yaml {oci-reg}
----

=== UI拡張をインストールします。

Rancher Manager UIを開き、「拡張」ページに移動します。

. Rancher Managerで、メニューバーの「拡張」をクリックします。
+
image::menubar-extension.png[メニューバーの拡張ボタン]
+
. 右上の縦3点リーダメニューを使用して「Rancherリポジトリを追加」を選択します。
+
image::rancher-repositories.png[Rancherリポジトリを管理するメニュー]
+
. 「公式Rancher拡張リポジトリを追加」を選択してリポジトリを追加します。
+
image::add-extensions-repositories.png[公式Rancherリポジトリを追加]
+
. 「追加」をクリックします。
+
. AIアシスタントカードを見つけて「インストール」をクリックします。
. バージョンを選択するか（デフォルトで最新を使用）し、「インストール」をクリックします。
+
image::config-repo-install.png[バージョンを選択するか、最新を使用します。]
+
. 拡張機能のインストールが完了したら、ページの上部に表示される「再読み込み」ボタンをクリックします。
